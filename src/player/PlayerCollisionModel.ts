import { CollisionUnitConfig } from 'src/collision/CollisionUnitConfig';
import { Vector3 } from 'three';

export const PlayerCollisionModel: CollisionUnitConfig = {
  size: new Vector3(5, 9.5, 5),
};
